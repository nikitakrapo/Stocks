<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nikitakrapo.android.stocks">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.nikitakrapo.android.stocks.presentation.StocksApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stocks">
        <activity android:name="com.nikitakrapo.android.stocks.presentation.ui.news.NewsArticleActivity" />
        <activity
            android:name="com.nikitakrapo.android.stocks.presentation.ui.search.SearchActivity"
            android:parentActivityName="com.nikitakrapo.android.stocks.presentation.ui.MainActivity" />
        <activity android:name="com.nikitakrapo.android.stocks.presentation.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>